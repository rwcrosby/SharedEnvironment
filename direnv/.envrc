# export BAT_THEME=Coldark-Dark               # Dark background shells
export BAT_THEME=GitHub                     # Light background shells

export UID_GID="$(id -u):$(id -g)"

export CONTAINER_REGISTRY=docker.io
export PORTAINER_REGISTRY=${CONTAINER_REGISTRY}

export SHAREDENVIRONMENT=$HOME/Projects/SharedEnvironment

services:

  agent:
      image: artifacts.lab.sstide.com/artifactory/kamino/import/portainer/agent:latest
      ports:
        - "9001:9001"
      volumes:
        - /var/run/docker.sock:/var/run/docker.sock
        - /var/lib/docker/volumes:/var/lib/docker/volumes
      restart: always
